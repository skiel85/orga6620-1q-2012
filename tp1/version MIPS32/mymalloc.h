/* FUNCIONES DE MEMORIA (mymalloc.h) */

#ifndef __MYMALLOC_H__
  #define __MYMALLOC_H__

	extern void *mymalloc(size_t size);
	//extern void *myrealloc(void * ptr, size_t size);
	extern void myfree(void * ptr);

#endif

/* FIN DE FUNCIONES DE MEMORIA (mymalloc.h) */
